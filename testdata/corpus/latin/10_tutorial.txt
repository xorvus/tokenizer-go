To install the package, first make sure you have the latest version of the runtime. Open a terminal and run the install command, then verify the installation by checking the version output. Next, create a configuration file in your project directory with your credentials. Start with the default settings and adjust them only after you understand each option. Finally, run your first command and inspect the logs to confirm everything initialized correctly.
